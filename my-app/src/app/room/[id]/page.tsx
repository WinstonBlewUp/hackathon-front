
import { ActionIcon, Badge, Button, Card, CardSection, Center, Flex, Group, Image, Input, Paper, SimpleGrid, Stack, Text, Title } from "@mantine/core";
import { IconBolt } from '@tabler/icons-react';
import placeholder from "../../../assets/image.png"
export default function Home() {
    return (
        <  >
            <Image src={placeholder.src} />
        </>
    );
}
